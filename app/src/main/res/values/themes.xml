<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.DausTodo" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- Primary brand color -->
        <item name="android:colorPrimary">@color/daus_blue</item>
        <item name="android:colorPrimaryDark">@color/daus_blue_variant</item>
        <item name="android:colorAccent">@color/daus_orange</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/white</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/daus_blue</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>
</resources>